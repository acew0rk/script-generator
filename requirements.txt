# yt-dlp downloads the video from a pasted link (e.g. TikTok) so it can be
# transcribed. Everything else in this app uses only the standard library.
# Unpinned on purpose: TikTok breaks older versions, so always take the latest.
yt-dlp
